set_default_toolchain("//build/toolchain/clang:clang")

cflags_cc = [
  "/std:c++17",
]

declare_args() {
  is_debug = false
  is_linux = host_os == "linux"
  is_mac = host_os == "mac"
  is_win = host_os == "win"
}

